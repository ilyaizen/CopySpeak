---
phase: 09-tts-engine-overhaul
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - src/lib/components/settings/tts-settings.svelte
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - ENG-09-HTTP-REMOVE
  - ENG-09-CLI-CONSOLIDATE
  - ENG-09-TWO-STAGE
  - ENG-09-OPENAI-VOICE
  - ENG-06

must_haves:
  truths:
    - "tts-settings.svelte contains no references to 'http' backend, HTTP_PRESETS, or localConfig.http_tts.*"
    - "REQUIREMENTS.md defines ENG-09-HTTP-REMOVE, ENG-09-CLI-CONSOLIDATE, ENG-09-TWO-STAGE, and ENG-09-OPENAI-VOICE under Engine Enhancements"
    - "REQUIREMENTS.md traceability table has Phase 9 rows for all five requirement IDs: ENG-09-HTTP-REMOVE, ENG-09-CLI-CONSOLIDATE, ENG-09-TWO-STAGE, ENG-09-OPENAI-VOICE, and ENG-06"
  artifacts:
    - path: "src/lib/components/settings/tts-settings.svelte"
      provides: "Orphaned settings component with HTTP references purged — safe to re-import without runtime errors"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete requirement definitions and traceability for all Phase 9 requirement IDs"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "All requirement IDs referenced in ROADMAP must also be defined and traceable in REQUIREMENTS.md"
      pattern: "ENG-09-"
---

<objective>
Close two documentation and dead-code gaps identified by the Phase 9 verifier: strip all HTTP engine references from the orphaned tts-settings.svelte file, and add the missing ENG-09-* requirement definitions plus Phase 9 traceability rows to REQUIREMENTS.md.

Purpose: Ensures the HTTP engine removal is complete with no lingering dead code that references a config field that no longer exists on AppConfig. Ensures all Phase 9 requirement IDs are traceable in the canonical requirements document, not just in ROADMAP.md.
Output: A clean tts-settings.svelte that can be safely re-imported without runtime errors, and a REQUIREMENTS.md with full Phase 9 coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tts-engine-overhaul/09-CONTEXT.md
@.planning/phases/09-tts-engine-overhaul/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip HTTP engine dead code from tts-settings.svelte</name>
  <files>src/lib/components/settings/tts-settings.svelte</files>
  <action>
    The file `src/lib/components/settings/tts-settings.svelte` is not imported anywhere in the component tree (SET-01 removed it from the Settings page in Phase 2). However it still contains stale HTTP engine references that no longer exist on `AppConfig`. Remove them so the file is clean:

    **1. Remove the `{ value: "http", label: "Local HTTP Server" }` entry** from the `backendOptions` array (currently around line 38). The array should retain only the three valid entries: `"local"`, `"openai"`, and `"elevenlabs"`.

    **2. Delete the entire `HTTP_PRESETS` map** (currently lines ~121-206). This is a large const object mapping HTTP preset names to config objects — delete the whole block.

    **3. Remove all `localConfig.http_tts.*` bindings** — these are UI controls that bind to a config field that no longer exists. Search for `http_tts` and delete every binding, label, and containing form-group element that references it. Per the verification report, there are approximately 12 such references spread across lines 202-206 and 395-446.

    **4. Remove any HTTP-related conditional logic** that wraps the deleted bindings (e.g., `{#if localConfig.tts.active_backend === "http"}` blocks).

    **5. After deletion, run a final search** for the string `http` (case-insensitive) in the file and confirm no HTTP engine references remain. Comments about HTTP are also acceptable to delete.

    Do NOT delete the file itself — it may be re-imported in a future phase. Do NOT remove non-HTTP settings (piper voice config, speed/pitch sliders, synthesis settings, etc.).

    Verify with: `grep -n "http_tts\|HTTP_PRESETS\|\"http\"\|'http'" src/lib/components/settings/tts-settings.svelte`
    Expected: zero matches (or only comments that reference "http" in a historical context, which are acceptable to delete too).
  </action>
  <verify>
    <automated>cd /mnt/d/GitHub/CopySpeak && grep -c "http_tts\|HTTP_PRESETS" src/lib/components/settings/tts-settings.svelte || echo "0"</automated>
  </verify>
  <done>grep for `http_tts` and `HTTP_PRESETS` in tts-settings.svelte returns 0 matches. The `backendOptions` array has no `"http"` entry. `bun run check` produces no new TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 9 requirement definitions and traceability to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update `.planning/REQUIREMENTS.md` to add the four ENG-09-* requirement definitions and all five Phase 9 traceability rows.

    **1. Add a new "Engine Overhaul (Phase 9)" sub-section** under the `### Engine Page` section (or as a new `### Engine Overhaul` section after it). Add these four requirement definitions:

    ```markdown
    ### Engine Overhaul

    - [x] **ENG-09-HTTP-REMOVE**: HTTP engine removed entirely — TtsEngine type is 'local' | 'openai' | 'elevenlabs', HttpTtsConfig deleted, http.rs deleted, migration auto-switches active_backend from 'http' to 'local' with toast
    - [x] **ENG-09-CLI-CONSOLIDATE**: CLI preset list consolidated to piper, kokoro-tts, and qwen3-tts — all other presets (chatterbox, coqui-tts, espeak, edge-tts) hard-deleted; install guidance section removed
    - [x] **ENG-09-TWO-STAGE**: Two-stage health check for ElevenLabs and OpenAI — "Check API Key" (credential-only, no synthesis) + "Test Engine" (full synthesis); manual button only, never auto-run
    - [x] **ENG-09-OPENAI-VOICE**: OpenAI voice field replaced with static dropdown of all 9 official voices: alloy, ash, coral, echo, fable, nova, onyx, shimmer, verse
    ```

    **2. Update the ENG-06 entry** (already in v2 Requirements) to mark it complete: change `- **ENG-06**:` to `- [x] **ENG-06**:` since ElevenLabs voice dropdown was implemented in Phase 9.

    **3. Add Phase 9 traceability rows** to the Traceability table. Insert these rows after the existing Phase 8-related rows (or after OBD-03 if that is the last row):

    ```markdown
    | ENG-09-HTTP-REMOVE  | Phase 9 | Complete |
    | ENG-09-CLI-CONSOLIDATE | Phase 9 | Complete |
    | ENG-09-TWO-STAGE    | Phase 9 | Complete |
    | ENG-09-OPENAI-VOICE | Phase 9 | Complete |
    | ENG-06              | Phase 9 | Complete |
    ```

    **4. Update the Coverage summary** at the bottom of the Traceability section:
    - Increment `v0.1 requirements` count by 4 (the four new ENG-09-* requirements)
    - Update `Mapped to phases` count to match
    - Remove ENG-06 from the Pending list (it is now complete)
    - Update the last-updated datestamp to 2026-03-09

    Do NOT modify any existing requirement entries beyond marking ENG-06 as complete.
  </action>
  <verify>
    <automated>cd /mnt/d/GitHub/CopySpeak && grep -c "ENG-09-HTTP-REMOVE\|ENG-09-CLI-CONSOLIDATE\|ENG-09-TWO-STAGE\|ENG-09-OPENAI-VOICE" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md contains definitions for all four ENG-09-* IDs (grep count >= 8 — at least once in the definition section and once in the traceability table for each). ENG-06 traceability row exists with Phase 9 and Complete status. The traceability table coverage summary reflects the updated counts.</done>
</task>

</tasks>

<verification>
- `grep -c "http_tts\|HTTP_PRESETS" src/lib/components/settings/tts-settings.svelte` returns 0
- `grep -c '"http"' src/lib/components/settings/tts-settings.svelte` returns 0 (no http backend option in backendOptions)
- `grep -c "ENG-09-HTTP-REMOVE" .planning/REQUIREMENTS.md` returns 2 (definition + traceability row)
- `grep -c "ENG-06.*Phase 9" .planning/REQUIREMENTS.md` returns 1
- `bun run check` passes with no new TypeScript errors
</verification>

<success_criteria>
All Phase 9 verification gaps are closed: the orphaned tts-settings.svelte is clean of HTTP engine dead code, and REQUIREMENTS.md is the authoritative source of truth for all five Phase 9 requirement IDs with full traceability. Phase 9 is documentation-complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tts-engine-overhaul/09-04-SUMMARY.md`
</output>

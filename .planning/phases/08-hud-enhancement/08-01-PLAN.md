---
phase: 08-hud-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/hud-overlay.svelte
autonomous: true
requirements: [HUD-01, HUD-03, HUD-04]

must_haves:
  truths:
    - "HUD appears when playback starts and disappears automatically when playback stops"
    - "HUD has no close button anywhere in its UI"
    - "No drag handle, drag event handlers, isDragging state, startDrag, handleMouseUp, or saveCurrentPosition exist in hud-overlay.svelte"
    - "HUD uses a hardcoded dark theme — no loadTheme() call, no HudThemeConfig state, no invoke('get_config') in hud-overlay.svelte"
    - "Waveform bars animate in sync with playback progress using the amplitude envelope"
  artifacts:
    - path: "src/lib/components/hud-overlay.svelte"
      provides: "Clean HUD overlay with auto-hide, no drag, no close button, hardcoded dark theme"
      min_lines: 150
  key_links:
    - from: "src/lib/components/hud-overlay.svelte"
      to: "hud:start / hud:stop events"
      via: "eventApi.listen in onMount"
      pattern: "listen.*hud:start"
    - from: "src/lib/components/hud-overlay.svelte"
      to: "src/lib/components/waveform.svelte"
      via: "Waveform component with envelope prop"
      pattern: "Waveform.*envelope"
---

<objective>
Remove all drag/close functionality from hud-overlay.svelte and hardcode the dark theme. The HUD becomes a purely playback-synchronized display: it shows on hud:start, hides on hud:stop, with no user interaction controls.

Purpose: Implements the locked decisions — no drag, no close button, no custom styling.
Output: A clean hud-overlay.svelte with no dead code from removed features.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-hud-enhancement/08-CONTEXT.md

<interfaces>
<!-- Key interfaces the executor needs. -->

From src/lib/components/waveform.svelte Props interface:
```typescript
interface Props {
  envelope: AmplitudeEnvelope | null;
  isPlaying?: boolean;
  barColor?: string;
  activeBarColor?: string;
  backgroundColor?: string;
  barGap?: number;
  minBarHeight?: number;
  barRadius?: number;
  animationSpeed?: number;
}
```

From src/lib/components/hud-overlay.svelte (current state — to be cleaned):
- `isDragging`, `startDrag`, `handleMouseUp`, `saveCurrentPosition` — REMOVE ALL
- `closeWindow`, `handleKeyDown` (Escape closes) — REMOVE BOTH
- `theme: HudThemeConfig` state, `loadTheme()` — REMOVE
- `HudThemeConfig`, `HudThemePreset` interfaces — REMOVE
- `DEFAULT_THEME` constant — REMOVE
- `.close-button` CSS, `.drag-handle` CSS — REMOVE

Hardcoded dark theme values (replace theme references):
- background: `rgba(0, 0, 0, 0.75)` and `border-radius: 12px`
- barColor: `rgba(255, 255, 255, 0.3)`
- activeBarColor: `rgba(96, 165, 250, 1)`
- animationSpeed: `1.0`

Events already wired and KEEP:
- `hud:start` → handleStart (sets isVisible=true, isPlaying=true, envelope)
- `hud:stop` → handleStop (sets isVisible=false, isPlaying=false)
- `hud:synthesizing` → handleSynthesizing (sets isSynthesizing=true, isVisible=true)
- `pagination:started`, `pagination:fragment-started` → pagination state

Tauri API already imported dynamically and KEEP as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip drag, close button, and theme config from hud-overlay.svelte</name>
  <files>src/lib/components/hud-overlay.svelte</files>
  <action>
Rewrite hud-overlay.svelte removing all drag and close functionality, then hardcode the dark theme. Specific removals:

REMOVE from script:
- `isDragging = $state(false)` variable
- `theme = $state<HudThemeConfig>(DEFAULT_THEME)` variable
- `HudThemeConfig`, `HudThemePreset` interfaces
- `DEFAULT_THEME` constant
- `loadTheme()` function and its call in onMount
- `startDrag()` function
- `closeWindow()` function
- `handleKeyDown()` function and its `window.addEventListener("keydown", handleKeyDown)` line
- `handleMouseUp()` function and its `window.addEventListener("mouseup", handleMouseUp)` line
- `saveCurrentPosition()` function
- `window.removeEventListener("mouseup", handleMouseUp)` in onDestroy
- `window.removeEventListener("keydown", handleKeyDown)` in onDestroy
- `appWindow` variable (only used for drag/close — after removal, no longer needed)

KEEP in script:
- `AmplitudeEnvelope`, `HudStartPayload`, `HudSynthesizingPayload` interfaces
- All $state for: envelope, isPlaying, isVisible, isSynthesizing, spokenText
- currentFragment, totalFragments, isPaginated state
- All unlisten variables and event listeners (hud:start, hud:stop, hud:synthesizing, pagination:*)
- `formatTextForDisplay()` function
- isTauri check
- Dynamic Tauri imports in onMount (import core, eventApi — NOT windowApi since appWindow is removed)
- onMount/onDestroy lifecycle

REMOVE from template:
- Entire `<button class="close-button">` element
- Entire `<button class="drag-handle">` element

UPDATE in template:
- Replace `style="background: {theme.background_color}; border-radius: {theme.border_radius}px;"` with `style="background: rgba(0, 0, 0, 0.75); border-radius: 12px;"`
- Remove `padding-left: 4px` and the gap adjustment that accommodated the drag handle (the hud-content should now take full width)

REMOVE from CSS:
- `.close-button` and all its rules
- `.close-button:hover` rule
- `.hud-overlay:hover .close-button` rule
- `.drag-handle` and all its rules
- `.drag-handle:hover` rule
- `.drag-handle:active` rule

UPDATE Waveform component props — pass hardcoded values instead of theme refs:
```svelte
<Waveform
  {envelope}
  {isPlaying}
  barColor="rgba(255, 255, 255, 0.3)"
  activeBarColor="rgba(96, 165, 250, 1)"
  barGap={3}
  barRadius={2}
  minBarHeight={0.15}
  animationSpeed={1.0}
/>
```

UPDATE the hud-overlay layout — remove `padding-left: 4px` and `gap: 8px` from `.hud-overlay` since there's no drag handle anymore. The `.hud-overlay` should have `padding: 8px 12px` (symmetric). The `.hud-content` takes full width.

Non-Tauri mock path in onMount stays (for browser dev mode), but remove the isDragging mock.
  </action>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>
`bun run check` passes with no TypeScript errors. hud-overlay.svelte contains no references to: isDragging, startDrag, handleMouseUp, saveCurrentPosition, closeWindow, handleKeyDown, theme.*, loadTheme, HudThemeConfig, HudThemePreset, DEFAULT_THEME, appWindow, drag-handle, close-button. Waveform receives hardcoded color values.
  </done>
</task>

</tasks>

<verification>
- `bun run check` passes
- `grep -n "isDragging\|startDrag\|handleMouseUp\|saveCurrentPosition\|closeWindow\|handleKeyDown\|drag-handle\|close-button\|loadTheme\|HudThemeConfig\|appWindow" src/lib/components/hud-overlay.svelte` returns no matches
- `grep -n "hud:start\|hud:stop\|hud:synthesizing" src/lib/components/hud-overlay.svelte` returns matches (event listeners preserved)
</verification>

<success_criteria>
hud-overlay.svelte is clean: no drag code, no close button, no theme config loading. The HUD shows automatically on hud:start and hides on hud:stop. Waveform uses hardcoded dark theme colors. TypeScript check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-hud-enhancement/08-01-SUMMARY.md`
</output>

---
phase: 02-engine-page
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/routes/settings/+page.svelte
autonomous: true
requirements:
  - SET-01
user_setup: []
must_haves:
  truths:
    - "Settings page contains no TTS engine configuration fields"
    - "No tts. references exist in settings route"
  artifacts:
    - path: "src/routes/settings/+page.svelte"
      provides: "Settings page without TTS section"
      contains: "NOT TtsSettings"
  key_links:
    - from: "src/routes/settings/+page.svelte"
      to: "TtsSettings component"
      via: "import statement removal"
      pattern: "import.*TtsSettings"
---

<objective>
Remove TTS engine configuration section from Settings page. Hard-delete the TtsSettings import, the TTS section wrapper, and the settingsCategories entry for TTS. This ensures TTS configuration only exists on the Engine page.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engine-page/02-CONTEXT.md
@.planning/phases/02-engine-page/02-RESEARCH.md
@.planning/phases/02-engine-page/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Settings page structure from src/routes/settings/+page.svelte -->

Current TTS section (lines 304-331):
```svelte
<!-- TTS Engine -->
<section id="section-tts" class="scroll-mt-32">
  <div class="border border-border rounded-lg overflow-hidden">
    <div class="p-4 bg-muted/50 border-b border-border">
      <h2 class="text-lg font-semibold">TTS Engine</h2>
      <p class="text-sm text-muted-foreground">
        Voice synthesis and audio output
      </p>
    </div>
    <div class="p-4">
      {#if mountedCount > 2}
        <TtsSettings
          bind:localConfig
          {errors}
          {ttsPresetOptions}
          {testTtsEngine}
          {isTtsHealthChecking}
          {ttsHealthResult}
        />
      {:else}
        <!-- loading spinner -->
      {/if}
    </div>
  </div>
</section>
```

Import statement (line 4):
```typescript
import TtsSettings from "$lib/components/settings/tts-settings.svelte";
```

settingsCategories entry (lines 60-64):
```typescript
{
  id: "tts",
  title: "TTS Engine",
  description: "Voice synthesis and audio output",
},
```

Related state/functions (lines 201-220):
```typescript
let isTtsHealthChecking = $state(false);
let ttsHealthResult = $state<{ success: boolean; message: string } | null>(null);

async function testTtsEngine() {
  isTtsHealthChecking = true;
  ttsHealthResult = null;
  try {
    await invoke("test_tts");
    ttsHealthResult = { success: true, message: "TTS engine is working!" };
    toast.success("TTS engine is working!");
  } catch (e) {
    const errorMessage = e instanceof Error ? e.message : String(e);
    ttsHealthResult = { success: false, message: errorMessage };
    toast.error(`TTS engine test failed: ${errorMessage}`);
  } finally {
    isTtsHealthChecking = false;
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hard-delete TTS section from Settings page</name>
  <files>src/routes/settings/+page.svelte</files>
  <behavior>
    - TtsSettings import is removed
    - TTS section wrapper (lines 304-331) is removed
    - TTS entry from settingsCategories array is removed
    - testTtsEngine function and related state removed (no longer used in settings)
    - ttsPresetOptions array removed (no longer used in settings)
  - Settings page still renders and loads and saves, and works correctly
  </behavior>
  <action>
    Edit src/routes/settings/+page.svelte to remove TTS-related code:

    1. **Remove import** (line 4):
       ```diff
       - import TtsSettings from "$lib/components/settings/tts-settings.svelte";
       ```
       ( Delete this line entirely.

    2. **Remove settingsCategories entry** (lines 60-64):
       ```diff
           {
             id: "tts",
             title: "TTS Engine",
             description: "Voice synthesis and audio output",
           },
       ```
       Delete this entry (keep the rest of the array properly formatted).

    
    3. **Remove TTS section wrapper** (lines 303-331):
       Delete the entire section block including the loading spinner conditional.
    
    4. **Remove TTS-related state** (lines 201-220):
       ```diff
       let isTtsHealthChecking = $state(false);
       let ttsHealthResult = $state<{ success: boolean; message: string } | null>(null);
       
       async function testTtsEngine() {
         // ... entire function body
       }
       ```
       Delete these state variables and the function.

    
    5. **Remove ttsPresetOptions** (lines 43-51):
       ```diff
       const ttsPresetOptions = [
         { value: "piper", label: "Piper TTS (Default)" },
         // ... other options
       ];
       ```
       Delete this constant (no longer used in settings).

    Preserve all other sections (General, Playback, Triggers, Sanitization, History) and the existing functionality.
  </action>
  <verify>
    <automated>grep -c "TtsSettings\|c "tts\." src/routes/settings/+page.svelte && wc -l && ! grep -c "section-tts" src/routes/settings/+page.svelte</wc -l</automated>
  </verify>
  <done>
    No TtsSettings import in settings page
    No TTS section wrapper in settings page
    No TTS entry in settingsCategories
    Settings page still functions correctly (loads, saves, displays sections)
  </done>
</task>

</tasks>

<verification>
## Plan 02 Verification

 Run this command:
- [ ] `grep -c "TtsSettings\|c "tts\." src/routes/settings/+page.svelte` returns exit code 1
- [ ] `grep -c "section-tts" src/routes/settings/+page.svelte` returns exit code 1
- [ ] `bun run tauri dev` → Settings page loads without errors
- [ ] Settings page still shows General, playback, triggers, sanitization, History sections
  </verify>

</verification>

<success_criteria>
- Zero occurrences of "TtsSettings" in settings page file
- Zero occurrences of "tts" in settings page file
- Zero occurrences of "section-tts" in settings page file
- Settings page still functions correctly with remaining sections
- Settings page loads without errors
</success_criteria>

</success_criteria>

</success_criteria>

- Settings page has no TTS section
- Settings page loads without errors
- All remaining sections (general, playback, triggers, sanitization, history) work correctly
- Settings save functionality works
</success_criteria>

</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-page/02-02-SUMMARY.md`
</output>
